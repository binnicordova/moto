export const ROUTES = {
    CLIENT: {
        HOME: "/client/home",
        RIDE: "/client/ride",
    },
    DRIVER: {
        DASHBOARD: "/driver/dashboard",
        RIDE: "/driver/ride",
    },
} as const;
